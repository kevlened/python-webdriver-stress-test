[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
selenium = "*"
chromedriver-binary = "*"

[dev-packages]

[requires]
python_version = "3.6"

[scripts]
parallel = "python test.py --type parallel"
concurrent = "python test.py --type concurrent"
